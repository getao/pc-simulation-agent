[project]
name = "claude-agent-code"
version = "0.1.0"
description = "Claude agents using Claude Code (via claude-agent-sdk)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk>=0.1.44",
    "python-docx>=1.2.0",
]
