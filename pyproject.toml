[project]
name = "pc-simulation-agent"
version = "0.1.0"
description = "Generate realistic Windows PC filesystem environments from user personas"
readme = "spec.md"
requires-python = ">=3.11"
dependencies = [
    "claude-agent-sdk>=0.1.44",
]
